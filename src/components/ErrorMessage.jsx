export default function ErrorMessage(){

    return (
      <div class="alert alert-primary alert-no-comment" role="alert" style={{width: 440 + "px"}}>
        Currently no comments available. 
      </div>
    );
}